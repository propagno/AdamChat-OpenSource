const http = require("http"); const options = { hostname: "backend-dev", port: 5000, path: "/api/auth/login", method: "POST", headers: { "Content-Type": "application/json" } }; const req = http.request(options, (res) => { console.log(`STATUS: ${res.statusCode}`); res.on("data", (chunk) => { console.log(`BODY: ${chunk}`); }); }); req.on("error", (e) => { console.error(`ERROR: ${e.message}`); }); req.write(JSON.stringify({ email: "test@example.com", password: "password123" })); req.end();
