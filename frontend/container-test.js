const http = require('http'); const options = { hostname: 'backend-dev', port: 5000, path: '/api/health', method: 'GET' }; console.log('Sending request to backend-dev:5000/api/health...'); const req = http.request(options, (res) => { console.log('Status:', res.statusCode); let data = ''; res.on('data', (chunk) => { data += chunk; }); res.on('end', () => { console.log('Response:', data); }); }); req.on('error', (e) => { console.error('Error:', e.message); }); req.end();
